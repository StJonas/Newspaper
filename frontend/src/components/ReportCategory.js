import {H2, Text} from "./Typography";

const ReportCategory = ({classes}) => {
    return (
        <div className={`${classes}`}>
            <H2>ReportCategory</H2>
            <Text>ReportCategory Content</Text>
        </div>
    );
};

export default ReportCategory;